<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">
    <Grid>
        <DockPanel LastChildFill="True"
                   AllowDrop="True"
                   Drop="DockPanel_Drop">
            <Menu DockPanel.Dock="Top"
                  VerticalAlignment="Top">
                <MenuItem Header="ファイル(_F)">
                    <MenuItem Header="開く(_O)"
                              Click="MenuItem_Open_Click"/>
                    <MenuItem Header="保存(_S)"
                              Click="MenuItem_SvaeAs_Click"/>
                    <MenuItem Header="閉じる(_C)"
                              Click="MenuItem_Close_Click"/>
                </MenuItem>
                <MenuItem Header="ツール(_T)">
                    <MenuItem Header="Blur(_B)"
                              x:Name="MenuBlur"
                              Click="Common_Click"/>
                    <MenuItem Header="GaussianBlur(_G)"
                              x:Name="MenuGaussianBlur"
                              Click="Common_Click"/>
                    <MenuItem Header="Laplacian(_L)"
                              x:Name="MenuLaplacian"
                              Click="Common_Click"/>
                    <MenuItem Header="Sobel(_S)"
                              x:Name="MenuSobel"
                              Click="Common_Click"/>
                    <MenuItem Header="Canny(_C)"
                              x:Name="MenuCanny"
                              Click="Common_Click"/>
                    <MenuItem Header="Dilate(_D)"
                              x:Name="MenuDilate"
                              Click="Common_Click"/>
                    <MenuItem Header="Erode(_E)"
                              x:Name="MenuErode"
                              Click="Common_Click"/>
                    <MenuItem Header="Gamma(_G)"
                              x:Name="MenuGamma"
                              Click="Common_Click"/>
                </MenuItem>
            </Menu>
            <StatusBar DockPanel.Dock="Bottom">
                <Label Name ="StatusBarText"
                       Content="file name"/>
            </StatusBar>
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">
                <Image Name ="Image"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Stretch="None"/>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>
